import * as React from 'react';
import Prototype from './Prototype';

class App extends React.Component {
  public render() {
    return (
      <div>
        <Prototype />
      </div>
    );
  }
}

export default App;
